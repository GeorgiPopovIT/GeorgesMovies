@model CommentServiceModel

@{
    var movieId = ViewContext.RouteData.Values["id"];
}
<form method="post">
    <div class="form-group col-md-4">
        <label asp-for="Message"  class="font-weight-bold">Enter comment</label>
        <input class="form-control"id="messageValue" asp-for="Message" placeholder="Comment...">
        <span asp-validation-for="Message" class="small text-danger"></span>

       <input id="btnSubmit"
              type="submit"
              class="btn btn-primary"
              value="Post"
              />

    </div>
</form>

@section Scripts{
    <script>
        @*$("#clickLink").each(function (el) {
            $(this).click(function () {

                var value = $(this).attr("#messageValue");
                var movieId = @movieId;
                var data = { message: value, movieId: movieId };

                $.ajax({
                    type: "POST",
                    url: "api/comment",
                    data: JSON.stringify(data),
                    success: function (data) {
                        console.log(data)
                    },
                    contentType:'application/json'
                });
            })
        });*@
        function btnClick() {

            $(document).ready(function () {
                $("#btnSubmit").click(function () {
                    alert("button");
                });
            });
        }
    </script>
}


